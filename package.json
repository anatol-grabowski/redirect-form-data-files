{
  "name": "redirect-form-data-files",
  "version": "1.0.1",
  "description": "Example: ``` const express = require('express') const { asyncExpress } = require('../async-express') const formidable = require('formidable') const { parseFormData, redirectFileParts } = require('../../util/handle-form-data')",
  "main": "handle-form-data.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/grabantot/redirect-form-data-files.git"
  },
  "bugs": {
    "url": "https://github.com/grabantot/redirect-form-data-files/issues"
  },
  "homepage": "https://github.com/grabantot/redirect-form-data-files#readme"
}
